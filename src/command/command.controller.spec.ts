import { CommandController } from './command.controller';

describe('CommandController', () => {
  it('can be instantiated', () => {
    expect(new CommandController()).toBeInstanceOf(CommandController);
  });
});
