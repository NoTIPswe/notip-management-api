import { ThresholdsService } from './thresholds.service';

describe('ThresholdsService', () => {
  it('can be instantiated', () => {
    expect(new ThresholdsService()).toBeInstanceOf(ThresholdsService);
  });
});
