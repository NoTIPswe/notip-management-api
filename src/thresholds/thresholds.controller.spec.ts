import { ThresholdsController } from './thresholds.controller';

describe('ThresholdsController', () => {
  it('can be instantiated', () => {
    expect(new ThresholdsController()).toBeInstanceOf(ThresholdsController);
  });
});
